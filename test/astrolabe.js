var expect    = require('chai').expect,
    astrolabe = require('../lib/astrolabe');

describe('astrolabe', function() {

    describe('with no arguments', function() {

        it('returns an empty array', function() {
            expect(true).to.equal(false);
        });
    });
});